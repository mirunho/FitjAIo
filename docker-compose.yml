services:
  app:
    build: .
    ports:
      - "80:8080"
    volumes:
      - fitjaio_data:/data
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - NODE_ENV=production
      - DB_PATH=/data/fitjaio.db
    restart: unless-stopped

volumes:
  fitjaio_data:
